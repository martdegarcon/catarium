import Image from "next/image";

/**
 * Renders a simple 404 error page heading.
 *
 * @returns The JSX element containing an `h1` with the text "404 error".
 */
export default function Home() {
  return (
    <h1>404 error</h1>
  );
}